# 公式サイト 引き継ぎメモ

2026-09-22 初回デザイン。`公式サイト計画書.md` と6枚のデザインカンプをもとに、`site/` を作り直した。
ゲーム本体（`site/play/`・ルートの `index.html`・`assets/`・`worker/`）は一切触っていない。

## ファイル構成

```
site/
├── index.html         トップ
├── rules.html         ルール・遊び方（操作方法のPC/スマホ切替・ミニ体験3種・FAQ）
├── characters.html    キャラ図鑑（先生/生徒タブ・検索・12件ずつページ送り・詳細パネル）
├── history.html       歴史（タイムライン）
├── ranking.html       ランキング（Worker の /api/leaderboard を表示）
├── about.html         このゲームについて
├── css/style.css      共通スタイル。先頭の :root に色・フォント・背景画像の変数をまとめてある
├── js/common.js       全ページ共通（アイコン・ドロワー・スクロール出現・最新アップデート3件）
├── js/rules.js        ルールページ（PC/スマホ切替・ミニ体験タブ）
├── js/characters.js   図鑑の描画
├── js/history.js      タイムラインの描画
├── js/ranking.js      ランキングの取得と描画
├── js/minigames.js    ミニ体験の中身（従来のまま）
├── js/characters-data.js  キャラ76体のデータ（従来のまま）
├── js/history-data.js     歴史24件のデータ（従来のまま）
└── img/
    ├── bg/window.svg    仮の背景：夕暮れの教室の窓
    ├── bg/corridor.svg  仮の背景：夕暮れの廊下
    ├── bg/board.svg     黒板の質感タイル
    ├── logo-mark.png    ヘッダーのロゴマーク（桜の紋）
    ├── favicon.png      タブのアイコン（ロゴマークと同じ絵）
    ├── favicon.svg      小さいサイズ用の簡略版（自動でこちらが優先される）
    └── ogp.png          SNSで共有したときのサムネ（未差し替え）
```

ビルド不要。`site/` をそのままブラウザで開けば動く（ローカル確認は `cd site && python3 -m http.server 8080`）。

## 中身を増やすとき

- キャラを増やす → `js/characters-data.js` に1件足す（id / side / name / tagline / color / cd / intro / abilities）
- 歴史を増やす → `js/history-data.js` の末尾に1件足す。トップの「最新のアップデート」にも自動で出る
- 色を変える → `css/style.css` 先頭の `:root` の変数
- ヘッダー・フッターは各HTMLに同じものが書いてある。直すときは6ファイルとも直す
- 「ほかの作品」（about.html）はURL未設定。決まったら `<div class="row">` を `<a class="row" href="...">` にする

## 背景画像（2026-09-22 差し替え済み）

`img/bg/` の corridor.jpg（夕暮れの廊下）・window.jpg（夕暮れの教室）・board.jpg（黒板の質感）を全ページで使っている。
ChatGPTで生成 → 1920px幅・品質78のJPGに変換して配置した（各440KB前後、黒板は900px角・143KB）。
作り直したいときは下のプロンプトで生成し、同じファイル名で上書きするだけでよい。`css/style.css` 先頭の変数を変えれば別ファイル名でも使える。

| 変数 | 使われる場所 | 推奨サイズ | 生成プロンプトの例 |
|---|---|---|---|
| `--bg-hero` | トップのいちばん上 | 1600×900 | 夕暮れの廊下（hero.jpg） |
| `--art-student` | 生徒側カード（トップ・ルール） | 900×506 | 逃げる女子生徒（howto-student.jpg） |
| `--art-teacher` | 先生側カード（トップ・ルール） | 900×506 | 定規を持つ先生（howto-teacher.jpg） |
| `--art-mode1` / `2` / `3` | 3つの対戦モード | 900×506 | 通常／2vs8／怪異退治（mode-*.jpg） |
| `--bg-corridor` | トップのヒーロー、生徒/先生カード、モードカード、スマホ操作のモック | 1920×1080（横長） | 下の「corridor.jpg」 |
| `--bg-window` | 下層ページの見出し帯、各ページ末尾のCTA帯、2vs8カード | 1920×1080（横長） | 下の「window.jpg」 |
| `--bg-board` | 黒板セクション・数字の帯・全カードの下地 | 900×900（正方形・つなぎ目なし） | 下の「board.jpg」 |

そのまま貼れるプロンプト（ChatGPTの画像生成用）：

corridor.jpg（横長 16:9）
```
アニメ映画の背景美術のような、夕暮れの誰もいない日本の高校の廊下。一点透視で奥に大きな窓があり、そこからオレンジ色の夕日が差し込んで床のタイルに長い光と反射を作っている。左側の窓からも斜めに光の筋。右側に灰色のロッカーが並ぶ。壁と天井は深い青緑の影。空気中にほこりが光る。人物なし、文字なし、看板なし。静かで少しノスタルジック。横長 16:9。
```

window.jpg（横長 16:9）
```
アニメ映画の背景美術のような、夕暮れの誰もいない日本の高校の教室。手前に木の机と椅子が並び、奥の壁一面の大きな窓の向こうに、燃えるようなオレンジと紫の夕焼け空、遠くの山並みと街の明かりが見える。窓枠のシルエットがはっきりしていて、夕日が机の上に暖かい光を落としている。黒板は画面に入れない。人物なし、文字なし。静かで少し切ない雰囲気。横長 16:9。
```

board.jpg（正方形 1:1）
```
深い緑色の学校の黒板を真正面から撮った質感テクスチャ。チョークの粉が薄く残った擦れ跡と、黒板消しで拭いたあとのムラがうっすらある。文字や絵は一切書かれていない。全面が黒板で枠や縁は見えない。タイルとして繰り返し敷けるよう、四辺で模様が途切れない。正方形。
```

書き換え例：
```css
--bg-corridor: url("../img/bg/corridor.jpg");
```
JPGは品質80前後・500KB以下を目安に。スマホの通信量に効く。Macなら次の1行で変換できる：
```
sips -s format jpeg -s formatOptions 78 --resampleWidth 1920 元画像.png --out corridor.jpg
```

### トップのタイトル（チョークの手書きロゴ）

トップのいちばん上の「チャイムが鳴ったら、はじまり。／放課後の居残り」は文字ではなく1枚の画像
（`img/hero-title.webp`）。差し替えるときは同じ名前で上書きするか、`index.html` の `hero__logo` の
`src` を変える。大きさは `css/style.css` の `.hero__logo img` の `width` で決めている。

★ `img/bg/hero.jpg`（トップの背景）には黒板の「また、みんなであそぼう。」と壁の「あの頃の放課後を
もう一度」が絵の中に描かれている。両端が切れないよう、トップだけ背景の動きを小さくしてある
（`css/style.css` の `.hero .bg-anim` と `@keyframes kenburns-hero`）。絵を差し替えて文字がない絵に
した場合は、この2行を消せば他のページと同じ大きな動きに戻る。

### 絵の明るさ・濃さを変えたいとき

絵の上には「文字を読めるようにするための薄い覆い」が乗っている。`css/style.css` の中で
`...__art::after`（カードの絵）、`.hero::before`（トップ）、`.page-hero::before`（下層ページの帯）、
`.cta-band::before` がそれだ。最後の数字（0〜1）を小さくすると絵が濃く、大きくすると文字が読みやすくなる。

絵そのものの鮮やかさは `.bg-anim` と `...__art::before` の `filter: saturate() contrast()` で調整する。

### 絵の見える位置を直したいとき

カードの絵は横長画像を縦長の枠に切り抜いて表示している。人物が切れてしまうときは `css/style.css` の次の行の
`--art-pos`（横位置 縦位置）を変えるだけでよい。`38% center` なら「画像の左から38%の位置を中央に置く」の意味。

```css
.role-card--blue, .rule-card--student { --art: var(--art-student); --art-pos: 38% center; }
```

さらに絵を足したいとき（任意）：
- キャラ絵のプロンプト例（背景透過で書き出す）
  - 生徒：`アニメ調のイラスト。日本の高校の制服を着た女子生徒が、夕暮れの校舎の廊下を必死に走って逃げている後ろ姿。全身、背景なし、透過PNG。文字なし。`
  - 先生：`アニメ調のイラスト。黒いロングコートの先生が、目を赤く光らせて長い定規を手に立っている不気味なシルエット。全身、正面やや斜め、背景なし、透過PNG。文字なし。`
- 生徒/先生カードの絵エリア（`.role-card__art` / `.rule-card__art`）に `<img class="role-card__figure" src="img/art/student.png" alt="">` を入れると、キャラ絵を下端合わせで重ねられる（背景透過PNG推奨）

## 「今すぐ遊ぶ」のリンク先

いまは全ページのボタンが、ゲーム本体の公開URLを直接指している。

```
https://tanakatakeshi811-spec.github.io/houkago-inokori/
```

自前のドメインを取って `/play` にゲームを置いたら、6つのHTMLの中のこのURLを `play/` に置換するだけでよい。
sitemap.xml にはゲームのURLを入れていない（外部のURLなので）。

## Cloudflare Pages に載せるとき

- ビルド出力ディレクトリ：`site`
- ゲーム本体を同じドメインに置きたい場合のみ、下のビルドコマンドと「今すぐ遊ぶ」のURL置換をセットで行う
- ビルドコマンド（ゲーム本体を /play にコピー）：`mkdir -p site/play && cp index.html site/play/ && cp -r assets site/play/`
  - いまは `site/play/` にコピー済みのものがリポジトリに入っているので、当面はビルドコマンドなしでも動く
- 本番ドメインが決まったら：
  - `sitemap.xml` / `robots.txt` / 各HTMLの `og:url` `og:image` の `https://houkago-inokori.com/` を一括置換
  - `worker/src/index.js` の CORS が `*` 以外になっていたら本番ドメインを追加（ランキングが空になる原因No.1）

## 動き（アニメーション）の仕組み

- 背景のゆっくりズーム・光の筋・チョークの粉：`<div class="bg-anim">` `<div class="bg-rays">` `<div class="dust">`（粉は `common.js` が生成）
- スクロールで現れる：`.reveal` を付けた要素。親に `data-stagger` を付けると子が順番に出る
- 見出しの下線が描かれる・数字のカウントアップ（`data-count`）・タイムラインの線が伸びる：`common.js` の IntersectionObserver が担当
- カードのホバー（絵が奥へズーム・浮き上がり）：`css/style.css` の「15. 動きと質感の追加」
- OSの「視差効果を減らす」がONの人には全部止まる（`prefers-reduced-motion`）

## 確認用の小技

- `ranking.html?sample=1` … ランキングをサンプルデータで表示（デザイン確認用）
- 2026-09-22 時点で Chromium と WebKit（Safari相当）の両方で、6ページ×スマホ/PC幅の横はみ出しゼロ・JSエラーゼロを確認済み
