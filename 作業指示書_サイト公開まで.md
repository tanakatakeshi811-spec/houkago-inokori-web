# 作業指示書｜公式サイトの受け取りから公開まで

この文書は、**「放課後の居残り」公式サイトのデザイン一式を受け取って、Cloudflare で公開するまで**の手順です。
作業するAIアシスタント（Codex / Claude など）に、このファイルごと読ませてください。

---

## 0. これは何か

`houkago-inokori` リポジトリの `site/` フォルダを、公式サイトとして作り直したものです。

- 全6ページ（トップ／ルール・遊び方／キャラ図鑑／歴史／ランキング／このゲームについて）
- 素のHTML・CSS・JavaScript のみ。ビルド不要、フレームワーク不使用
- 背景の一枚絵・チョークのタイトル・ロゴマークを新規に用意して組み込み済み
- **ゲーム本体（リポジトリ直下の `index.html`・`assets/`・`worker/`・`site/play/`）には一切触れていません**

構成・中身の増やし方・色や画像の差し替え方は、同じフォルダの **`引き継ぎメモ.md`** に全部書いてあります。
作業前にそちらも読んでください。

---

## 1. ファイルを置く

受け取った `site/` の中身を、リポジトリの `site/` に上書きします。

```
houkago-inokori/
├── index.html          ← ゲーム本体。触らない
├── assets/             ← ゲーム用素材。触らない
├── worker/             ← Cloudflare Worker。触らない
└── site/               ← ★ここを今回のもので置き換える
    ├── index.html  rules.html  characters.html  history.html  ranking.html  about.html
    ├── css/style.css
    ├── js/（common / rules / characters / history / ranking / minigames / 各データ）
    ├── img/（logo-mark.png  favicon.png  favicon.svg  ogp.png  hero-title.webp  bg/ 9枚）
    ├── sitemap.xml  robots.txt
    ├── 引き継ぎメモ.md
    └── 作業指示書_サイト公開まで.md（このファイル）
```

**`site/play/` は今回の配布物に含めていません。**リポジトリに既にあるものをそのまま残してください。
（ゲーム本体のコピーで、音声ファイルを含めて30MB以上あるため）

置いたらローカルで一度開いて確認します。

```
cd site && python3 -m http.server 8080
```

→ ブラウザで `http://localhost:8080/` 。6ページすべてリンクで回れること、画像が出ることを見てください。

---

## 2. Cloudflare Pages に載せる

Cloudflare のダッシュボード →  Workers & Pages → Create → Pages → GitHubリポジトリ `houkago-inokori` を接続。

| 設定項目 | 値 |
|---|---|
| Production branch | `main` |
| Framework preset | なし（None） |
| Build command | 空欄のまま |
| Build output directory | **`site`** |

これで `site/` の中身がそのまま公開されます。ビルドは不要です。

**ゲーム本体も同じドメインで配信したい場合のみ**、Build command に次の1行を入れます。

```
mkdir -p site/play && cp index.html site/play/ && cp -r assets site/play/
```

その場合は「4. 公開後にやること」のURL差し替えも合わせて行ってください。

---

## 3. 独自ドメインをつなぐ（取得済みの場合のみ）

Pages プロジェクト → Custom domains → ドメインを追加。
Cloudflare でドメインを取っていれば DNS は自動で設定されます。SSL（https）も自動です。

---

## 4. 公開後にやること

### 4-1. サイト内のURLを本番のものに直す

いま各ページの `<head>` には、仮のドメイン `https://houkago-inokori.com/` が入っています。
本番ドメインが決まったら、**6つのHTMLファイルと `sitemap.xml` `robots.txt`** の中のこの文字列を、
実際のドメインに一括置換してください。（`og:url` `og:image` とサイトマップのURLに使われています）

### 4-2. 「今すぐ遊ぶ」のリンク先

現在は全ページのボタンが、ゲームの現在の公開URLを直接指しています。

```
https://tanakatakeshi811-spec.github.io/houkago-inokori/
```

「2.」でゲーム本体を同じドメインの `/play` に置いた場合は、6つのHTMLの中のこのURLを `play/` に置換してください。
置かない場合はそのままで動きます。

### 4-3. ランキングのCORS設定（確認）

ランキングページは Worker の `/api/leaderboard` を叩きます。
`worker/src/index.js` の `CORS` は現在 `'access-control-allow-origin': '*'`（全許可）なので、**そのままで動きます**。
もし将来ここを特定ドメインに絞るときは、公式サイトのドメインを必ず許可リストに入れてください。
入れ忘れると「ランキングが空っぽ」という形で失敗します。

### 4-4. Google Search Console

登録してサイトマップ（`https://<ドメイン>/sitemap.xml`）を送信すると、検索に載りやすくなります。

---

## 5. 触るときの注意

- **ヘッダーとフッターは6つのHTMLに同じものが書いてあります。**直すときは6ファイルとも直してください
- キャラクターを増やす → `js/characters-data.js` に1件足すだけ
- 更新履歴を増やす → `js/history-data.js` の末尾に1件足すだけ（トップの「最新のアップデート」にも自動で出ます）
- 色を変える → `css/style.css` の先頭にある `:root` の変数
- 背景画像を差し替える → `img/bg/` の同じファイル名で上書き
- `ranking.html?sample=1` を開くと、ランキングをサンプルデータで表示できます（デザイン確認用）

---

## 6. 動作確認済みの環境（2026-09-22 時点）

Chromium と WebKit（Safari相当）の両方で、6ページ × スマートフォン幅・PC幅を確認しています。

- 横方向のはみ出し：なし
- JavaScriptのエラー：なし
- 読み込みに失敗するファイル：なし
- ランキングAPIからの実データ取得：成功

画像を含めたサイト全体の容量は約2.6MBです（ゲーム本体を除く）。
